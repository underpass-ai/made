# Versioned, provider-neutral C6 evaluation specification.
# The runner expands each family deterministically into the declared number of
# cases. Inputs are fixtures, not evidence of model quality.
version = "c6.1"
seed = "made-c6-local"
families = [
  { name = "software_change", count = 34, oracle = "deterministic" },
  { name = "incident_diagnosis", count = 33, oracle = "deterministic" },
  { name = "evidence_research", count = 33, oracle = "deterministic" },
]
