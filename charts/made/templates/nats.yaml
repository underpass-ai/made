{{- if .Values.messaging.nats.embedded.enabled }}
{{- $natsName := include "made.natsFullname" . }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $natsName }}
  labels:
    {{- include "made.natsLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "made.natsSelectorLabels" . | nindent 4 }}
  ports:
    - name: client
      port: 4222
      targetPort: 4222
      protocol: TCP
    {{- if .Values.messaging.nats.embedded.monitoring.enabled }}
    - name: monitor
      port: 8222
      targetPort: 8222
      protocol: TCP
    {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $natsName }}
  labels:
    {{- include "made.natsLabels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "made.natsSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "made.natsSelectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: nats
          image: "{{ .Values.messaging.nats.embedded.image.repository }}:{{ .Values.messaging.nats.embedded.image.tag }}"
          imagePullPolicy: {{ .Values.messaging.nats.embedded.image.pullPolicy }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          {{- if or .Values.messaging.nats.embedded.jetstream.enabled .Values.messaging.nats.embedded.monitoring.enabled }}
          args:
            {{- if .Values.messaging.nats.embedded.jetstream.enabled }}
            - "-js"
            - "--store_dir=/data/jetstream"
            {{- end }}
            {{- if .Values.messaging.nats.embedded.monitoring.enabled }}
            - "-m"
            - "8222"
            {{- end }}
          {{- end }}
          ports:
            - name: client
              containerPort: 4222
              protocol: TCP
            {{- if .Values.messaging.nats.embedded.monitoring.enabled }}
            - name: monitor
              containerPort: 8222
              protocol: TCP
            {{- end }}
          {{- if .Values.messaging.nats.embedded.jetstream.enabled }}
          volumeMounts:
            - name: data
              mountPath: /data
          {{- end }}
          resources:
            {{- toYaml .Values.messaging.nats.embedded.resources | nindent 12 }}
          livenessProbe:
            tcpSocket:
              port: client
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: client
            initialDelaySeconds: 2
            periodSeconds: 5
      {{- if .Values.messaging.nats.embedded.jetstream.enabled }}
      volumes:
        - name: data
          {{- if .Values.messaging.nats.embedded.jetstream.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ $natsName }}-data
          {{- else }}
          emptyDir:
            medium: Memory
            sizeLimit: {{ .Values.messaging.nats.embedded.jetstream.persistence.size }}
          {{- end }}
      {{- end }}
{{- if and .Values.messaging.nats.embedded.jetstream.enabled .Values.messaging.nats.embedded.jetstream.persistence.enabled }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $natsName }}-data
  labels:
    {{- include "made.natsLabels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- with .Values.messaging.nats.embedded.jetstream.persistence.storageClass }}
  storageClassName: {{ . | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.messaging.nats.embedded.jetstream.persistence.size }}
{{- end }}
{{- end }}
