[workspace]
resolver = "2"
members = [
    "crates/made-core",
    "crates/made-api",
    "crates/made-app",
    "crates/made-adapters",
    "crates/made-embedded",
    "crates/made-proto",
    "crates/made-mcp-proto",
    "crates/made",
    "crates/made-tests-integration",
    "crates/made-e2e-runner",
    "crates/made-mcp",
    "crates/made-consumer-smoke",
]

[workspace.package]
version = "0.2.0"
edition = "2021"
rust-version = "1.97"
license = "Apache-2.0"
authors = ["Tirso Garcia Ibañez"]
repository = "https://github.com/underpass-ai/made"

[workspace.lints.rust]
unsafe_code = "forbid"

[workspace.lints.clippy]
pedantic = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"
doc_markdown = "allow"
cast_precision_loss = "allow"
cast_possible_truncation = "allow"
cast_possible_wrap = "allow"
cast_sign_loss = "allow"
float_cmp = "allow"
missing_fields_in_debug = "allow"
too_many_arguments = "allow"

[workspace.dependencies]
# async
tokio = { version = "1.40", features = ["full"] }
tokio-stream = "0.1"
futures = "0.3"
async-trait = "0.1"

# errors
thiserror = "1.0"
anyhow = "1.0"

# serde
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"

# net
url = "2.5"

# time / ids
time = { version = "0.3", features = ["serde", "serde-human-readable", "macros", "formatting", "parsing"] }
uuid = { version = "1.10", features = ["v4", "serde"] }

# observability
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
opentelemetry = "0.32"
opentelemetry_sdk = { version = "0.32", features = ["rt-tokio"] }
opentelemetry-otlp = { version = "0.32", default-features = false, features = [
    "grpc-tonic",
    "trace",
    "tls",
    "tls-roots",
] }
opentelemetry-semantic-conventions = "0.32"
tracing-opentelemetry = "0.33"
# Prometheus client: explicit Registry + typed metric handles, injected
# through the `MetricsRecorderPort` adapter (no global recorder). The
# protobuf push-gateway feature is off — only the text exposition format
# is needed for the `/metrics` scrape endpoint.
prometheus = { version = "0.13", default-features = false }

# gRPC
tonic = { version = "0.12", features = ["tls", "tls-roots"] }
tonic-build = "0.12"
prost = "0.13"
prost-types = "0.13"

# NATS
async-nats = "0.50"

# SQL / persistence
redb = "4.1"
sqlx = { version = "0.8", default-features = false, features = [
    "runtime-tokio-rustls",
    "postgres",
    "macros",
    "migrate",
    "time",
    "uuid",
    "json",
] }

# config
figment = { version = "0.10", features = ["env", "toml"] }

# testing
mockall = "0.13"
tokio-test = "0.4"

# benchmarking
criterion = { version = "0.5", features = ["async_tokio"] }

# crypto / hashing
sha2 = "0.10"

# schema validation
jsonschema = { version = "0.17", default-features = false }

# internal
made-core = { path = "crates/made-core", version = "0.2.0" }
made-api = { path = "crates/made-api", version = "0.2.0" }
made-app = { path = "crates/made-app", version = "0.2.0" }
made-adapters = { path = "crates/made-adapters", version = "0.2.0", default-features = false }
made-embedded = { path = "crates/made-embedded", version = "0.2.0" }
made-proto = { path = "crates/made-proto", version = "0.2.0" }
made-mcp = { path = "crates/made-mcp", version = "0.2.0", default-features = false }
made-mcp-proto = { path = "crates/made-mcp-proto", version = "0.2.0" }
made-tests-integration = { path = "crates/made-tests-integration", version = "0.2.0" }

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
