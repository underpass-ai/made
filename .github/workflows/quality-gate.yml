name: quality-gate

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: quality-gate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  contract:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          fetch-depth: 0

      - name: Install buf
        run: bash scripts/ci/install-buf.sh

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '20'

      - name: Install AsyncAPI CLI
        run: bash scripts/ci/install-asyncapi.sh

      - name: Run contract gate
        run: bash scripts/ci/contract-gate.sh

  rustfmt:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9
        with:
          toolchain: 1.97.1
          components: rustfmt
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Check domain vocabulary boundary
        run: bash scripts/ci/domain-vocabulary-boundary.sh

  embedded-boundary:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9
        with:
          toolchain: 1.97.1
          components: clippy
      - name: Check embedded dependency boundary
        run: bash scripts/ci/embedded-dependency-boundary.sh
      - name: Lint embedded feature boundary
        run: cargo clippy -p made-embedded --all-targets --locked -- -D warnings

  embedded-sqlite-gates:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9
        with:
          toolchain: 1.97.1
          components: clippy
      # The opt-in SQLite engine: every store contract on it, plus the
      # two-processes-one-store scenario it exists for. The default jobs
      # above prove it did not leak into the default build.
      - name: Enforce embedded SQLite engine gates
        run: bash scripts/ci/embedded-sqlite-gates.sh
      - name: Lint embedded MCP backend
        run: cargo clippy -p made-mcp --all-targets --no-default-features --features embedded --locked -- -D warnings
      - name: Test embedded MCP stdio
        run: cargo test -p made-mcp --all-targets --no-default-features --features embedded --locked
      - name: Smoke-test Codex plugin bundle
        run: bash scripts/ci/made-plugin-smoke.sh

  clippy:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    needs: [contract]
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9
        with:
          toolchain: 1.97.1
          components: clippy
      - name: Install protoc
        run: bash scripts/ci/install-protoc.sh
      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@779680da715d629ac1d338a641029a2f4372abb5
      - name: Run clippy
        # Enable every provider / transport / persistence feature
        # explicitly so feature-gated adapters (agent-anthropic,
        # agent-openai, agent-vllm, grpc, nats, redb, kmp, otel) are
        # linted. A feature CI never turns on is code CI never checks —
        # `otel` was outside this list while the trace bridge silently
        # stopped adopting incoming traceparents.
        # `container-tests` is deliberately excluded because the
        # separate `integration` workflow covers it with a real Docker
        # runtime.
        run: |
          cargo clippy --workspace --all-targets --locked \
            --features made-embedded/redb \
            --features made-adapters/agent-anthropic \
            --features made-adapters/agent-openai \
            --features made-adapters/agent-vllm \
            --features made-adapters/redb \
            --features made-adapters/kmp \
            --features made-adapters/otel \
            --features made/otel \
            -- -D warnings

  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    needs: [contract]
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9
        with:
          toolchain: 1.97.1
      - name: Install protoc
        run: bash scripts/ci/install-protoc.sh
      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@779680da715d629ac1d338a641029a2f4372abb5
      - name: Run tests
        # Same feature set as clippy — exercise every provider,
        # transport and persistence module. `container-tests` stays on
        # the separate `integration` workflow (real Docker).
        #
        # `kmp_live_kernel.rs` is ignored by default and therefore
        # reports `0 passed; N ignored` here, which is the honest
        # count: the kernel is another repository's binary and this job
        # does not build it. It used to run and skip, which reported
        # `N passed` for tests that had started nothing. What runs here
        # is the adapter's own half of the contract; the kernel's half
        # is checked wherever that binary exists, and cannot report
        # success without it.
        run: |
          cargo test --workspace --locked \
            --features made-embedded/redb \
            --features made-adapters/agent-anthropic \
            --features made-adapters/agent-openai \
            --features made-adapters/agent-vllm \
            --features made-adapters/redb \
            --features made-adapters/kmp \
            --features made-adapters/otel \
            --features made/otel

  container-image:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    needs: [rustfmt, clippy, test]
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - name: Verify Docker
        run: docker version
      - name: Build container image
        run: bash scripts/ci/container-image.sh

  helm-chart:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    needs: [contract]
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - name: Install Helm
        run: bash scripts/ci/install-helm.sh
      - name: Lint Helm chart
        run: bash scripts/ci/helm-lint.sh

  benches-compile:
    # Criterion benches don't run on every PR (too noisy, wrong
    # signal/noise ratio) but they must keep compiling — a stale
    # bench silently breaks every experiment's reproducer.
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    needs: [contract]
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9
        with:
          toolchain: 1.97.1
      - name: Install protoc
        run: bash scripts/ci/install-protoc.sh
      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@779680da715d629ac1d338a641029a2f4372abb5
      - name: Compile benches
        run: bash scripts/ci/bench-compile.sh

  publish-dry-run:
    # Catches packaging regressions for the two crates that go to
    # crates.io (`made-mcp-proto` + `made-mcp`) before they hit
    # the publish-distribution workflow. No registry token is
    # required — `cargo publish --dry-run` and `cargo package -l`
    # only validate metadata and the file list.
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    needs: [contract]
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9
        with:
          toolchain: 1.97.1
      - name: Install protoc
        run: bash scripts/ci/install-protoc.sh
      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@779680da715d629ac1d338a641029a2f4372abb5
      - name: Publish dry-run
        run: bash scripts/ci/publish-dry-run.sh
