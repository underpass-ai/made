---
# End-to-end runner as a Kubernetes Job.
#
# Launched after the MADE chart is installed in the target
# Kubernetes namespace. The CI script rewrites the image reference and,
# when needed, injects `imagePullSecrets` so the Job can pull from a
# cluster-accessible registry such as GHCR.
apiVersion: batch/v1
kind: Job
metadata:
  name: made-e2e-runner
  labels:
    app.kubernetes.io/name: made-e2e-runner
    app.kubernetes.io/part-of: made-e2e
spec:
  # The runner is intended to run exactly once; a restart on failure
  # would mask the real error from the CI logs.
  backoffLimit: 0
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: made-e2e-runner
    spec:
      restartPolicy: Never
      containers:
        - name: runner
          image: made-e2e-runner:e2e
          imagePullPolicy: Never
          env:
            - name: MADE_ENDPOINT
              value: http://made:50055
            - name: MADE_SEED_SPECIALTY
              value: triage
            - name: MADE_E2E_SCENARIOS
              value: cluster-connectivity,ceremony-diagram,daily-standup,technical-debate,sprint-planning,speaker-talk-qa
            - name: RUST_LOG
              value: info
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 500m
              memory: 128Mi
